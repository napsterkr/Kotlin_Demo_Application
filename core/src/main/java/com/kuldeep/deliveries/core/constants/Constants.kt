package com.kuldeep.deliveries.core.constants

import com.kuldeep.posts.core.BuildConfig


object Constants {
    val API_URL = BuildConfig.BASE_URL

    object Posts {
        val DB_NAME = "kuldeep_db"
    }
}